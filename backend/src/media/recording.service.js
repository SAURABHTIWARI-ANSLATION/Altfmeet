// recording.service.js (stub)
export function startRecording(meetingId) {
  console.log(`Start recording for meeting ${meetingId} (not implemented yet)`);
}

export function stopRecording(meetingId) {
  console.log(`Stop recording for meeting ${meetingId} (not implemented yet)`);
}
